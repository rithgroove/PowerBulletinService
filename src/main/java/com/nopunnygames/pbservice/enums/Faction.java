package com.nopunnygames.pbservice.enums;

/**
 * Power Bulletin faction assigned to a card identity.
 */
public enum Faction {
    /** Hero-aligned card identity. */
    HERO,
    /** Villain-aligned card identity. */
    VILLAIN,
    /** Civilian-aligned card identity. */
    CIVILIAN
}
